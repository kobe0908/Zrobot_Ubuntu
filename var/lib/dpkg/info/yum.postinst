#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/yum-updatesd" ]; then
	update-rc.d yum-updatesd defaults >/dev/null
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d yum-updatesd start || exit $?
	else
		/etc/init.d/yum-updatesd start || exit $?
	fi
fi
# End automatically added section
# Automatically added by dh_pysupport
if which update-python-modules >/dev/null 2>&1; then
	update-python-modules  yum.public yum.private
fi
# End automatically added section
