{
	"format": "Lava-Test Test Definition 1.0",
	"test_id": "wifi-enablement-test",
	"install": {
		"steps": ["bzr branch lp:~linaro-foundations/linaro-ubuntu/lava-test-wifi-enablement"],
		"deps": ["bzr", "wpasupplicant", "isc-dhcp-client", "wireless-tools", "net-tools"]
	},
	"run": {
		"steps": ["cd lava-test-wifi-enablement; sudo bash -x ./run-test.sh"]
	},
	"parse" : {
		"pattern": "(?P<test_case_id>[a-zA-Z0-9_-]+):\\s(?P<result>\\w+)",
		"fixupdict": {
		    "PASS": "pass",
		    "FAIL": "fail"
		}
	}
}
