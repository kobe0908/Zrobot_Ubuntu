project(camcar)
cmake_minimum_required(VERSION 2.6)
set(INC ./src)
add_subdirectory(src)
aux_source_directory(. DIR_SRCS)
include_directories(${INC})
add_library(camcar SHARED ${DIR_SRCS})
target_link_libraries(camcar cam_car)
